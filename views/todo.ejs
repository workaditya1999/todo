<!DOCTYPE html>
<html>

<head>
    <title>Aditya's Personal Online Todo List</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            font-family: "Poppins", sans-serif;
            background: radial-gradient(circle at top, #1a1a1a, #000000 70%);
            display: flex;
            justify-content: center;
            align-items: center;
            color: #f5f5f5;
        }

        .container {
            width: 480px;
            background: rgba(20, 20, 20, 0.7);
            padding: 40px;
            border-radius: 18px;
            border: 1px solid rgba(255, 215, 0, 0.2);
            box-shadow: 0 0 35px rgba(255, 215, 0, 0.08);
            backdrop-filter: blur(14px);
        }

        h1 {
            text-align: center;
            font-size: 32px;
            font-weight: 900;
            margin-bottom: 35px;
            background: linear-gradient(90deg, #ffd700, #fbe7a1);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 1px;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            background: rgba(255, 255, 255, 0.05);
            padding: 16px 18px;
            border-radius: 12px;
            margin-bottom: 14px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: 600;
            font-size: 16px;
            transition: 0.25s ease;
            border: 1px solid transparent;
        }

        li:hover {
            transform: translateX(6px);
            border: 1px solid rgba(255, 215, 0, 0.3);
            background: rgba(255, 255, 255, 0.10);
        }

        .actions a {
            text-decoration: none;
            color: #ffd700;
            font-size: 20px;
            margin-left: 14px;
            transition: 0.25s ease;
        }

        .actions a:hover {
            color: #fff4b8;
            transform: scale(1.25);
        }

        form {
            margin-top: 28px;
            display: flex;
            gap: 12px;
        }

        input[type="text"] {
            flex: 1;
            padding: 14px;
            font-size: 15px;
            border-radius: 10px;
            border: 1px solid rgba(255, 215, 0, 0.3);
            background: rgba(255, 255, 255, 0.08);
            color: #f5f5f5;
            font-weight: 600;
            outline: none;
            transition: 0.25s ease;
        }

        input[type="text"]:focus {
            border-color: #ffd700;
            background: rgba(255, 255, 255, 0.12);
        }

        input[type="text"]::placeholder {
            color: #d6d6d6;
        }

        input[type="submit"] {
            background: linear-gradient(135deg, #ffd700, #f3c953);
            border: none;
            color: #000;
            padding: 14px 22px;
            font-size: 15px;
            font-weight: 700;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.25s ease;
        }

        input[type="submit"]:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(255, 215, 0, 0.3);
        }
    </style>
</head>

<body>

    <div class="container">

        <h1>Aditya’s Personal Online Todo List</h1>

        <ul>
            <% todolist.forEach(function(todo, index) { %>
                <li>
                    <span><%- todo %></span>
                    <div class="actions">
                        <a href="/todo/<%= index %>">✎</a>
                        <a href="/todo/delete/<%= index %>">✘</a>
                    </div>
                </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <input type="text" name="newtodo" placeholder="Add a new classy task..." autofocus />
            <input type="submit" value="Add" />
        </form>

    </div>

</body>

</html>
