<!DOCTYPE html>
<html>

<head>
    <title>Aditya's Personal Online Todo List</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Poppins", Arial, sans-serif;
            background: linear-gradient(135deg, #1a73e8, #6a11cb);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            width: 460px;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px);
            padding: 35px;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.25);
            color: white;
        }

        h1 {
            text-align: center;
            font-size: 30px;
            font-weight: 800;
            margin-bottom: 30px;
            background: linear-gradient(90deg, #fff, #e3dffd);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            background: rgba(255, 255, 255, 0.18);
            padding: 14px 16px;
            border-radius: 12px;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: transform 0.2s ease, background 0.3s ease;
            font-size: 16px;
            font-weight: 600;
        }

        li:hover {
            transform: scale(1.03);
            background: rgba(255, 255, 255, 0.28);
        }

        .actions a {
            text-decoration: none;
            color: #ffffff;
            font-size: 20px;
            font-weight: bold;
            margin-left: 12px;
            transition: color 0.2s ease, transform 0.2s ease;
        }

        .actions a:hover {
            color: #ffcb05;
            transform: scale(1.2);
        }

        form {
            margin-top: 25px;
            display: flex;
            gap: 12px;
        }

        input[type="text"] {
            flex: 1;
            padding: 12px;
            font-size: 15px;
            border-radius: 10px;
            border: none;
            outline: none;
            background: rgba(255, 255, 255, 0.25);
            color: white;
            font-weight: 600;
        }

        input[type="text"]::placeholder {
            color: #eaeaea;
        }

        input[type="submit"] {
            background: linear-gradient(135deg, #ffcb05, #ff9f1c);
            border: none;
            color: black;
            padding: 12px 20px;
            font-size: 15px;
            font-weight: 700;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.2s ease;
        }

        input[type="submit"]:hover {
            transform: scale(1.06);
            background: linear-gradient(135deg, #ffe16b, #ffbf3f);
        }
    </style>
</head>

<body>

    <div class="container">

        <h1>Aditya’s Personal Online Todo List</h1>

        <ul>
            <% todolist.forEach(function(todo, index) { %>
                <li>
                    <span><%- todo %></span>
                    <div class="actions">
                        <a href="/todo/<%= index %>">✎</a>
                        <a href="/todo/delete/<%= index %>">✘</a>
                    </div>
                </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <input type="text" name="newtodo" placeholder="Add a new task..." autofocus />
            <input type="submit" value="Add" />
        </form>

    </div>

</body>

</html>
