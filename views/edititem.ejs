<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Todo</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #a8d5e5, #74c0d8);
            font-family: "Poppins", sans-serif;
        }

        .card {
            background: rgba(255, 255, 255, 0.65);
            backdrop-filter: blur(8px);
            padding: 40px;
            width: 450px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            margin-bottom: 25px;
            font-size: 26px;
            font-weight: 600;
            color: #0b5a6e;
        }

        label {
            font-size: 16px;
            display: block;
            margin-bottom: 10px;
            color: #0b5a6e;
            text-align: left;
        }

        .input-box {
            width: 100%;
            padding: 12px 15px;
            font-size: 15px;
            border-radius: 10px;
            border: none;
            outline: none;
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .btn-save {
            width: 100%;
            padding: 12px;
            background: #0d6efd;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.2s;
        }

        .btn-save:hover {
            background: #0b5ed7;
        }
    </style>

</head>

<body>

    <div class="card">
        <h1>Edit Your Task</h1>

        <form action="/todo/edit/<%= todoIdx %>" method="post">
            <input type="hidden" name="_method" value="PUT">

            <label for="editTodo">Current Task:</label>
            <input class="input-box" type="text" id="editTodo" name="editTodo"
                placeholder="<%- todo %>" autofocus>

            <button type="submit" class="btn-save">Save Changes</button>
        </form>
    </div>

</body>
</html>

